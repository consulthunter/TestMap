# Analysis

This directory is for post-processing data exported from TestMap runs.

The main application produces the source data. Analysis scripts and notebooks should stay separate from the runtime so notebook dependencies do not leak into the .NET tool.
